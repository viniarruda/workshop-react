const colors = {
  primary: "#7159c1",
  secondary: "#9b49c1",
  default: "#fff"
};

const viewports = {
  smartphone: "360px",
  tablet: "720px",
  desktop: "1280px"
};

const theme = Object.freeze({
  colors,
  viewports
});

export default theme;
